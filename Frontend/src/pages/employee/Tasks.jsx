import WorkerTasks from "../../components/worker/WorkerTasks";

// Employee task workspace: assigned split tasks, task details, start task,
// and the report submission flow (with optional attachment).
function EmployeeTasks() {
    return <WorkerTasks roleLabel="Employee" />;
}

export default EmployeeTasks;
